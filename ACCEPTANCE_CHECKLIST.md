# 警政視訊服務改版驗收

## 這次範圍

- 民眾入口、客服與管理端採一致藍色風格；入口依參考圖重建響應式版型。正式警徽及背景尚未提供，目前為清楚標示的示意素材，不宣稱已達像素級一致。
- 後台維持案件列表與詳情，支援姓名／證號／案件編號搜尋及狀態篩選。視訊與案件訊息在寬螢幕並排，手機堆疊。
- 只接收目前選中案件，不自動跳案；選取已開通案件會自動進入接收模式，不開啟我方裝置。
- 攝影機與麥克風獨立手動控制。切案時若有本地裝置發布，先確認，再關閉裝置，進入新案件時仍只接收。
- 民眾入口「我要視訊報案」：未開通等待審核，已開通則要求裝置權限並開始視訊。重載恢復案件不擅自要求鏡頭權限。
- 預設自己＋等待客服兩格；每位實際發布影像／語音的客服有獨立格，僅接收的客服不佔民眾端格子。
- 訊息與附件依時間合併，輪詢不重建視訊或清除草稿；上傳失敗不假裝成功。目前 base64 API 單檔 3 MiB，中文檔名保留。
- 錄影暫不接入，沒有背景瀏覽器錄影；連線時顯示「視訊筆錄進行中｜錄影尚未啟用」。未來 Agora Cloud Recording 啟用並實際回報錄製成功，才可顯示錄影中。

## 自動驗證

`npm test`：模擬 DOM、REST 與 Agora SDK 的生命週期／案件流程測試。

`npm run check`：JavaScript 語法檢查。

這些測試不等同實際 Agora 雲端、鏡頭、權限或行動瀏覽器驗收。

## 上線前實機測試

1. 民眾、客服甲、客服乙使用三個不同瀏覽器設定檔或不同裝置。客服使用不同帳號，避免同一 Agora UID 互相取代。
2. 民眾新申請顯示待審核；管理員審核後，重新按入口按鈕應直接要求鏡頭／麥克風。
3. 客服選取案件即可看見民眾，不要求客服裝置權限；其他案件開始視訊不得強制切案。
4. 客服甲只開麥克風：民眾聽見聲音且顯示語音格，不得看見我方攝影機；再開鏡頭則顯示影像。
5. 客服乙開啟視訊：民眾應顯示自己＋甲＋乙三格；乙離線不應清掉甲。關閉／重開攝影機亦需正常重訂閱。
6. 客服有發布時切案：取消保留原通話；確認關閉原案装置；新案不應自動發布我方影音。
7. 測拒絕鏡頭、只允許麥克風、裝置忙碌、網路中斷與重試。錯誤留在案件頁，無假成功；聲音被瀏覽器阻擋可按「播放聲音」。
8. 兩端上傳圖片／PDF 等小於 3 MiB 檔案，雙方對話應出現檔名、大小、上傳人與時間；重新整理不重複。超限顯示明確原因。
9. 驗證 390px 手機與 1440px 桌面：兩格／多人畫面、搜尋、訊息捲動、附件控制可操作。
10. 所有端錄影狀態必須如實顯示未啟用，不得因已有視訊或「筆錄中」就顯示影片已保存。

## 部署與交付注意

- 這次以 Vercel `/api` + Supabase 為主要執行環境。備援 `server.js` 的舊 SQLite／Socket.IO 路由不是這次新 REST 流程的等價實作，不能直接切換成正式備援。
- 同時發布三份 HTML、`public/styles.css`、`public/video-session.js`、`public/app.js` 與本次 API 文案／檔案修正；只發布 HTML 會缺少新控制器。
- 尚未提供正式警徽／背景；置換前保留素材占位說明。
- Cloud Recording、儲存區、REST 憑證、費用、錄影通知及保存期限待正式交付前另行設定與驗收，本次沒有開通或扣款。
- **真實個資上線前須另做安全驗收**：既有 Storage bucket 為 public（持有檔案網址即可讀取），須改為授權下載／私有儲存方案；檢查歷史 GET 登入與預設管理員邏輯、僅姓名證號的認證方式。不要把原型的可用性測試視為安全認證。
- 先前曾公開貼出的 Supabase secret／Agora certificate 必須撤銷並換新，不要在聊天或 Git 儲存新密鑰。